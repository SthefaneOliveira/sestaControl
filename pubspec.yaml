name: meu_app
description: Um aplicativo Flutter com autenticação e gerenciamento de usuários usando Moor.
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^1.7.0
  moor_flutter: ^4.0.0
  moor_generator: ^4.0.0
  provider: ^6.0.1
  material_design_icons_flutter: ^7.0.7296
  flutter_native_splash: ^1.2.1

flutter:
  assets:
    - lib/assets/cesta.png
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.10